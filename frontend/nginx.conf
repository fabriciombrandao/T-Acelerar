server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Arquivo único, sem rotas client-side — sempre serve o mesmo index.html.
    location / {
        try_files $uri /index.html;
    }
}
